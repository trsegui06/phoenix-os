# Domain conventions

Each domain owns its business rules, use cases and types. Domains do not import UI components or perform database queries directly. Data access is introduced through dedicated adapters when a domain is implemented.
